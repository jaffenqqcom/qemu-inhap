# 鸿蒙移植说明及编译指导

这份文档把《鸿蒙移植改造方案》和《QEMU编译指导》合到一起，主要讲三件事：
一是对 QEMU 基础版本改了哪些地方、为什么改，二是怎么编译出鸿蒙能用的
`libqemu-system-aarch64.so`（含工具链、程序、库的依赖要求），三是怎么把它集成进
鸿蒙程序——坑比较多，都列在最后。

## 第一部分　移植说明

### 一、目标与基线

- **目标**：把 QEMU 交叉编译成鸿蒙（OHOS）可用的 `libqemu-system-aarch64.so` 共享库，
  被鸿蒙 HAP 通过 `dlopen` 加载，在独立线程中运行 `-M virt` 的 ARM64 Linux guest。
- **基线**：本仓库 `master`，QEMU 11.1.50（2026-08-26 附近）。
- **Guest 架构**：arm64 Linux guest（`aarch64-softmmu`，`-M virt`）。不用 `-M microvm`——
  该机型仅 x86_64 专属。
- **产物形态**：外部依赖（glib/zlib/slirp 等）全部静态链入，`.so` 的 `NEEDED` 只剩
  `libc.so`，拷一个文件就能用。
- **功能**：串口（PL011）、initramfs rootfs、virtio-9p 文件夹共享、virtio-serial 命令桥、
  virtio-net + slirp 用户态网络、`fsdev-add` QMP 动态挂载。
- **路线依据**：社区已验证方案 `harmoninux/qemu`（产物即 `libqemu-system-aarch64.so`，
  经 N-API 被 HAP 加载）；本方案在其基础上针对 11.1.50 与本地环境重新制定。

### 二、修改了什么

#### 2.1 核心改造：产物从可执行文件变为共享库

QEMU 默认产物是可执行文件（`qemu-system-aarch64`），而鸿蒙 HAP 需要 `dlopen`
一个 `.so`。因此：

- **`meson_options.txt`**：新增 `libqemu` 选项（默认 `disabled`）。
- **`meson.build`**：`static_library` 增加 `pic:`；新增 `shared_library` 目标产出
  `libqemu-system-aarch64.so`。

`libqemu` 选项默认关闭，Linux/其他平台的构建路径完全不变，只有 OHOS 构建显式开启。
共享库默认导出 `main` 符号，集成方 `dlsym` 就能拿到入口。

#### 2.2 OHOS 平台适配

- **`util/cpuinfo-aarch64.c`**：`HWCAP2_CSSC` 用 `#ifdef` 保护——OHOS musl sysroot
  没定义这个宏，直接写会编译不过。
- **`configs/devices/aarch64-softmmu/ohos.mak`**：最小设备集
  （`ARM_VIRT` + `VIRTIO` + `VIRTIO_MMIO/PCI` + `VIRTIO_BLK/NET/SERIAL` + `VIRTFS` + `VIRTIO_9P`）。
  HAP 内嵌的虚拟机只需要串口、9p 共享、网络、命令桥，外设越少体积越小；
  配合 `--without-default-devices`（minikconf allnoconfig）。

#### 2.3 fsdev-add QMP 动态挂载补丁

新增 `fsdev-add` QMP 命令（QEMU 官方不支持运行时新增 9p export）：

- `qapi/fsdev.json`（新建）— `FsdevAdd` 结构 + `fsdev-add` 命令（`boxed: true`）。
- `qapi/qapi-schema.json`、`qapi/meson.build` — 注册 fsdev 模块。
- `fsdev/qemu-fsdev.c` — `qmp_fsdev_add` 实现（QemuOpts → `qemu_fsdev_add` 入全局链表）。
- `fsdev/qemu-fsdev-dummy.c` — 9p 未编译时的 stub。

宿主的工作目录要在运行时才挂进 guest（`device_add virtio-9p-pci`），启动时用
`-fsdev` 固化路径不合适，所以加了这条命令。细节见 `HANDOFF-fsdev-add.md`。

#### 2.4 构建脚本（scripts/ohos/，全部新增）

- `env.sh` — 工具链/依赖环境、生成修正 prefix 的 `.pc`、生成 clang/pkg-config 包装脚本、
  glib/zlib 静态化。
- `configure-ohos.sh` — 最小化 `--disable` + `--enable-slirp` + `--disable-pixman` +
  `--without-default-devices` + `-Dlibqemu=enabled` + `-Dslirp:default_library=static`。
- `build-ohos.sh` — `sync-src` + `ninja`。
- `sync-src.sh` — 共享盘源码复制到本地副本。
- `build-zlib-pic.sh` — 编译 PIC 版静态 zlib。
- `dlopen-test.c` — 真机 dlopen 冒烟测试。

这些脚本有两个由来：一是共享盘（virtiofs）对非 root 的 chmod/chown/utimensat
返回 EPERM，构建必须在本地磁盘副本上进行；二是 harmonybrew 的 zlib 静态库不是
PIC，链不进 .so，得自己编一份。

### 三、技术路线与裁剪原则

```
宿主机（openEuler aarch64）
  ├─ OHOS NDK clang ── 交叉编译 ──▶ libqemu-system-aarch64.so（含 TCG JIT）
  ├─ harmonybrew glib/zlib/pcre2/gettext/libffi（aarch64 musl）── 静态链入
  └─ meson（system=linux, cpu_family=aarch64）

鸿蒙设备（arm64-v8a HAP）
  ├─ libentry.so（NAPI）：dlopen("libqemu-system-aarch64.so") + dlsym + 起线程
  ├─ ArkTS：构造 -M virt 命令行，调 native.startVM()
  └─ 权限：ALLOW_WRITABLE_CODE_MEMORY / LOAD_INDEPENDENT_LIBRARY /
     IGNORE_LIBRARY_VALIDATION / CUSTOM_SANDBOX / INTERNET
```

**裁剪原则**：TCG、virt 机型、virtio、serial、块设备这些核心功能都保留。
`--disable-*` 只关掉 OHOS 平台本来就没有的后端（图形栈、KVM/vhost/USB、远程存储、
多余的镜像格式等），属于平台适配，不是砍功能。

---

## 第二部分　编译指导

### 四、环境与依赖要求

#### 4.1 工具链要求

- 宿主：Linux **aarch64**（aarch64 glibc）。
- **OHOS NDK**：`/mnt/linux_share/workspace/commandline-linux-arm64/sdk/default/openharmony/native/llvm`
  - **来源**：`https://github.com/jaffenqqcom/commandline-linux-arm64.git`
  - `aarch64-unknown-linux-ohos-clang` / `-clang++` 包装器（自带
    `--target=aarch64-linux-ohos --sysroot`）。
  - sysroot 位于 `native/sysroot`，含 `libc.so`（musl）。
  - 注意：NDK 的 `llvm-strip` 不可靠（见第十一节）。

#### 4.2 程序依赖要求

- `pkg-config`、`ninja`、`python3`、`make`（宿主系统自带）。
- meson：QEMU 11.1.50 要求 `meson >= 1.5.0`（宿主 `pip3 install meson`，或修正
  harmonybrew meson 的 shebang）。
- 构建用 wrap 子项目源码（libslirp、dtc、berkeley-softfloat-3 等）在 configure 时
  由 meson 自动下载；本机已有缓存（`/tmp/qemu-ohos/qemu-src/subprojects`）。

#### 4.3 库依赖要求

- **harmonybrew**（`/mnt/linux_share/.harmonybrew`，aarch64 OHOS/musl）：
  - glib 2.88.3（含 glib/gobject/gmodule/gio）
  - pixman 0.46.4（已禁用，不链接）
  - pcre2 10.47（glib 依赖）
  - gettext libintl（glib 依赖）
  - libffi（glib 依赖）
  - zlib-ng-compat（非 PIC，不用其 `.a`）
- **zlib-1.3.1 源码**：`/mnt/linux_share/workspace/ohcode-qemu/zlib-1.3.1`
  （或 `ZLIB_SRC_TAR` 指定），用于编译 PIC 静态库。
- **构建根**：`QEMU_OHOS_BUILD=/tmp/qemu-ohos`，必须是**本地磁盘**（非 virtiofs 共享盘）。

> 目前这台开发机上，环境、依赖都已经配好了：OHOS NDK、harmonybrew 依赖库、
> zlib-1.3.1 源码、构建根 `/tmp/qemu-ohos` 都在，不用再下载或安装什么。按第五节的
> 步骤直接编译就行；wrap 子项目源码本地已有缓存，也不会重新走网络。

### 五、编译步骤

脚本全部在仓库 `scripts/ohos/` 下。

#### 步骤 1：configure（首次构建，或改了配置/设备集之后）

```bash
cd /mnt/linux_share/workspace/qemu-inhap
bash scripts/ohos/configure-ohos.sh
```

`configure-ohos.sh` 内部：
- `source env.sh`：设置工具链、生成修正 prefix 的 `.pc` 覆盖文件（glib/zlib 指向
  静态库）、生成 clang/pkg-config 包装脚本。
- 以最小化 `--disable` + `--enable-slirp` + `--without-default-devices`
  `--with-devices-aarch64=ohos` + `-Dlibqemu=enabled` + `-Dslirp:default_library=static`
  + `--disable-pixman` 运行 configure。
- 这步 meson 会把 wrap 子项目源码（libslirp、dtc、berkeley-softfloat-3 等）从网上拉下来，
  并生成 `build.ninja`。

#### 步骤 2：zlib PIC 静态库（首次构建，或 `/tmp/qemu-ohos` 被清空后）

```bash
bash scripts/ohos/build-zlib-pic.sh
```

> 背景：harmonybrew 的 `libz.a` 不是 `-fPIC` 编译，链进共享库会报
> `relocation R_AARCH64_ADR_PREL_PG_HI21 ... recompile with -fPIC`。这个脚本把
> zlib-1.3.1 用 OHOS clang 以 `-fPIC` 重新编成静态库，输出
> `/tmp/qemu-ohos/zlib-pic/lib/libz.a`（幂等：已存在就跳过）。

#### 步骤 3：构建

```bash
bash scripts/ohos/build-ohos.sh libqemu-system-aarch64.so
```

`build-ohos.sh` 内部：`sync-src.sh`（复制最新源码到本地副本）→ `ninja`。
不带参数构建全部目标；传 `libqemu-system-aarch64.so` 只构建共享库。

#### 步骤 4：strip（交付时，可选但建议）

```bash
cp /tmp/qemu-ohos/build/libqemu-system-aarch64.so /tmp/qemu-ohos/build/libqemu-system-aarch64-stripped.so
/usr/bin/strip /tmp/qemu-ohos/build/libqemu-system-aarch64-stripped.so
```

> 注意：别用 OHOS NDK 的 `llvm-strip`（原因见第十一节）。

#### 步骤 5：验证

```bash
F=/tmp/qemu-ohos/build/libqemu-system-aarch64-stripped.so
readelf -d "$F" | grep NEEDED      # 期望只有 [libc.so]
nm -D "$F" | grep -E " main$|qmp_fsdev_add|qemu_fsdev_add"
strings "$F" | grep -E "virtio-(net|9p|serial)-pci"
file "$F"                          # ELF 64-bit shared object, aarch64
```

正常的话，NEEDED 只剩 `libc.so`，`main`、`qmp_fsdev_add` 都在导出表里，
`virtio-net-pci`、`virtio-9p-pci`、`virtio-serial-pci` 字符串也都在。strip 后大约 15.7 MB。

### 六、配置说明

#### 6.1 设备集（configs/devices/aarch64-softmmu/ohos.mak）

`CONFIG_ARM_VIRT` + `CONFIG_VIRTIO` + `CONFIG_VIRTIO_MMIO` + `CONFIG_VIRTIO_PCI` +
`CONFIG_VIRTIO_BLK` + `CONFIG_VIRTIO_NET` + `CONFIG_VIRTIO_SERIAL` +
`CONFIG_VIRTFS` + `CONFIG_VIRTIO_9P`。配合 `--without-default-devices`（allnoconfig）。

#### 6.2 裁剪的功能（configure-ohos.sh 的 --disable）

- 图形/显示：sdl gtk vnc curses opengl virglrenderer spice 等
- 音频：alsa pipewire pa jack coreaudio oss sndio
- 镜像格式：qcow1 qed vdi vpc vmdk bochs cloop dmg vvfat 等（留 raw/qcow2）
- 网络后端：af-xdp l2tpv3 vde netmap vmnet bpf passt（留 user/slirp）
- 远程存储/加密/迁移/模块/工具/USB/vhost/KVM 等

#### 6.3 静态链入的依赖

- glib（`libglib-2.0.a`）+ pcre2 + libintl + libffi（harmonybrew，PIC）
- zlib（`/tmp/qemu-ohos/zlib-pic/lib/libz.a`，自编译 PIC）
- libslirp（`subprojects/slirp/libslirp.a`，wrap 构建，`-Dslirp:default_library=static`）
- 系统库 libm.a / libutil.a（musl sysroot）
- pixman 已 `--disable-pixman` 移除

---

## 第三部分　集成指导

### 七、dlopen 加载与入口

HAP 的 NAPI 层 `dlopen("libqemu-system-aarch64.so")`，`dlsym` 取入口后开独立线程运行。
入口符号为 **`main`**（QEMU 的 `qemu-system-aarch64` main）。

```c
typedef int (*qemu_main_fn)(int, char **);
void *h = dlopen("libqemu-system-aarch64.so", RTLD_NOW | RTLD_GLOBAL);
qemu_main_fn qemu_main = (qemu_main_fn)dlsym(h, "main");
std::thread([&]{ qemu_main(argc, argv); }).detach();
```

仓库内有冒烟测试 `scripts/ohos/dlopen-test.c`（真机/交叉编译后运行）。

> 注意：OHcode 侧 `qemu_runner.cpp` 是 `dlsym(..., "qemu_system_entry")`，跟我们
> 导出的 `main` 对不上。要直接替换 OHcode 的 `.so`，得在源码里给 `main` 加个别名，
> 或者让 OHcode 那边把符号名改过来。

### 八、命令行参数（对齐 OHcode 用法）

```text
-M virt -cpu cortex-a57 -m 512M
-kernel <Image> -initrd <rootfs.cpio.gz>
-append "console=ttyAMA0,115200 rdinit=/sbin/init"
-display none -monitor none
-serial file:<console.log>
-netdev user,id=net0,hostfwd=tcp:127.0.0.1:39001-:7681
-device virtio-net-pci,netdev=net0,romfile=
-fsdev local,security_model=mapped-file,id=fsdev0,path=<宿主工作目录>
-device virtio-9p-pci,id=fs0,fsdev=fsdev0,mount_tag=usershare
-device virtio-serial-pci,id=virtio-serial0
-chardev socket,path=<hostcmd.sock>,server=on,wait=off,id=hostcmd0
-device virtserialport,chardev=hostcmd0,bus=virtio-serial0.0,name=ohcode.hostcmd
```

这些 PCI 设备依赖 `CONFIG_VIRTIO_PCI=y`（已在 `ohos.mak` 启用）。

### 九、动态挂载工作目录（fsdev-add QMP）

顺序：

1. 启动 QEMU 带 QMP 通道：
   `-qmp unix:path=<sock>,server=on,wait=off`
2. 创建 fsdev：
   `{"execute":"fsdev-add","arguments":{"id":"fsdev0","path":"<真实路径>","security-model":"mapped-file"}}`
3. 热插拔 virtio-9p 设备：
   `{"execute":"device_add","arguments":{"driver":"virtio-9p-pci","id":"fs0","fsdev":"fsdev0","mount_tag":"user0"}}`
4. guest 内挂载：
   `mount -t 9p -o trans=virtio,version=9p2000.L user0 /mnt/user0`

> 注意：`mount_tag` 要唯一（user0、user1...）；动态加的 fsdev 不会随 `-fsdev`
> 命令行参数删掉，卸载靠 `device_del` + 可选 fsdev 清理。

### 十、HAP 集成与权限

- 把 `libqemu-system-aarch64.so` 放进 HAP 的 `libs/<abi>/` 资源目录。
- `module.json5` 的 `requestPermissions` 需声明（产物**无鸿蒙签名**，且经 dlopen 加载）：
  - `LOAD_INDEPENDENT_LIBRARY`（kernel 级）——加载非应用自带 .so
  - `IGNORE_LIBRARY_VALIDATION`（kernel 级）——跳过 .so 校验
  - `ALLOW_WRITABLE_CODE_MEMORY`（kernel 级，**仅平板/PC/2in1，手机被驳回**）——
    TCG JIT 可写可执行内存
  - `CUSTOM_SANDBOX`、`READ_WRITE_USER_FILE`（system 级）——文件路径访问
  - `INTERNET`、`GET_NETWORK_INFO`（normal 级）——virtio-net 网络
- 每项权限声明三要素（name/reason/usedScene），reason 指向字符串资源。
- 旧版依赖库（libslirp/libglib/libpixman/libz/libintl/libpcre2 等）**不再需要**，
  静态链入后可不随 HAP 分发。

### 十一、踩过的坑

1. **zlib / pixman 静态库非 PIC**：harmonybrew 的 `.a` 不能链进 .so
   （`recompile with -fPIC`）。zlib 已脚本化自编译（`build-zlib-pic.sh`）；
   pixman 直接 `--disable-pixman`。
2. **llvm-strip 失效**：OHOS NDK 的 `llvm-objcopy` 被改名 `.bak`、只剩 725 字节脚本，
   普通 strip 不删 debug 段。用 `/usr/bin/strip` 或
   `llvm-strip --strip-debug --strip-unneeded`。
3. **.pc prefix 是设备路径**：harmonybrew 的 `.pc` 指向
   `/storage/Users/currentUser/.harmonybrew`（本机不存在）。env.sh 用 `sed` 换成
   宿主机真实路径。
4. **virtiofs EPERM**：共享盘不能直接构建（chmod/chown/utimensat 被拒），必须
   `sync-src` 到本地磁盘再构建。
5. **VIRTIO_PCI 需要 CONFIG_PCI**：`CONFIG_PCI` 由 virt 机器 select 链
   （gpex → PCI_EXPRESS → PCI）自动提供，ohos.mak 只需 `CONFIG_VIRTIO_PCI=y`。
6. **OHOS sysroot 缺宏**：`HWCAP2_CSSC` 需 `#ifdef` 保护（util/cpuinfo-aarch64.c）。
7. **宿主不能直接 dlopen**：宿主是 aarch64 glibc，`.so` 是 OHOS musl，NEEDED 的
   `libc.so` 在 glibc 宿主上解析不到。宿主侧只能做静态检查（readelf/nm）；运行验证
   必须真机（或 qemu-aarch64 用户态 + musl 环境）。
8. **JIT 可执行内存（真机）**：OHOS 沙箱默认拒 W+X。需
   `ALLOW_WRITABLE_CODE_MEMORY` 权限 + TCG 的 code_gen_buffer `mmap` 追加
   `MAP_EXECUTABLE`(=MAP_JIT)。手机拿不到权限，需 `--enable-tcg-interpreter`（TCI）
   纯解释变体。
9. **seccomp SIGSYS 硬杀**：OHOS 应用 seccomp 白名单外的 syscall（`openat2`、
   `memfd_create`、`clone3` 等）会被硬杀（非错误码）。真机跑最小 guest，用
   `cppcrash-com.*.log` 定位 `SIGSYS(syscall=N)`，白名单外在代码层改绕行。
10. **glib 静态链接的 gmodule 风险**：glib 静态链入 + dlopen 场景下 gmodule 机制可能
    行为异常。若遇到，回退为动态打包 glib。
11. **信号处理器冲突**：QEMU 安装自己的 SIGSEGV/SIGBUS 处理器（TCG guard page），
    宿主 App 若也装信号处理器会相互干扰。
12. **`-M microvm` 不存在**：该机型仅 x86_64；arm64 用 `-M virt`。
13. **QAPI 补丁的字段约定**（改 fsdev.json 时）：QAPI 可选 str 字段生成 `char *`，
    用 `!= NULL` 判断，只有 bool 字段才有 `has_*`；member 名用连字符
    （`security-model`），生成的 C 字段自动转下划线。

---

## 附：产物清单（scripts/ohos/output/）

- `libqemu-system-aarch64.so` —— 独立共享库（strip 后约 15.7 MB，NEEDED 仅 libc.so）。
- 旧依赖库（libslirp/libglib/libpixman/libz/libintl/libpcre2/libgmodule）：
  **已不需要**，静态链入后不随 HAP 分发，可清理。
- 注：若实际部署走**动态链接形态**（当前 zcoder HAP 即如此，见下节），
  libslirp/libglib 等 6 个依赖 so 仍需随 HAP 分发，其来源与许可按下节清单核对。

---

## 附：实际部署 so 集合（动态链接形态）的来源与许可

> 本文档主体（§6.3、§十、上一节产物清单）描述的是**全静态链入**的目标形态
> （NEEDED 仅 `libc.so`）。而 zcoder 当前实际部署的是一份**动态链接形态**产物：
> `crates/gpui_ohos/depend/ohos-qemu-agent/images/` 下有未 strip 的
> `libqemu-system-aarch64.so`（约 67 MB）+ 6 个依赖 so，由 `script/bundle-ohos`
> 一并拷入 HAP `hap/entry/libs/arm64-v8a/`。本节按 2026-09-03 的核查
> （ELF 动态段解析 + 二进制特征 + md5 对比 + brew 安装收据）记录这套集合的
> 依赖关系、来源与许可证，供溯源、审计与合规使用。

### A. 集合构成与依赖关系

6 个依赖 so 全部是 `libqemu-system-aarch64.so` 的运行时依赖，**与 virtio-fs 无关**
（virtio-fs 后端以 Rust crate 形态静态链入 cmd-agent，不产出独立 so）。ELF
DT_NEEDED 实测依赖树：

```
libqemu-system-aarch64.so
 ├── libglib-2.0.so.0 ──┬── libintl.so.8     （glib 的 gettext 国际化）
 │                      └── libpcre2-8.so.0  （glib 的 GRegex）
 ├── libpixman-1.so.0    （帧缓冲位图运算）
 ├── libslirp.so.0       （用户态网络栈，-netdev user）
 ├── libz.so.1           （zlib 压缩）
 └── libc.so             （OHOS musl，系统自带，无需打包）
```

libqemu 直接依赖 glib/pixman/slirp/z 这 4 个；libintl、libpcre2 是 glib 拉进来的
传递依赖。其余 5 个库只直接链 `libc.so`。

### B. 来源核查结论

| 文件 | 上游项目（版本） | 来源 | 判定证据 |
|---|---|---|---|
| libqemu-system-aarch64.so | QEMU 11.1.50（qemu-inhap） | **本地编译** | 内嵌路径 `/home/user/qemu-ohos/build`；.comment 含 OHOS clang 15.0.4、OpenEuler clang 17.0.6、LLD 17.0.6 |
| libslirp.so.0 | freedesktop libslirp **4.7.0** | **本地编译**（QEMU 构建树内） | .comment 与 libqemu 逐字一致；内嵌 `/src/arp_table.c` 等源码路径；libqemu RUNPATH 含 `$ORIGIN/subprojects/slirp` |
| libglib-2.0.so.0 | GLib 2.88.3 | 预编译 bottle | md5 与 Cellar 一致；`INSTALL_RECEIPT.json` 记 `built_as_bottle: True` |
| libintl.so.8 | gettext 1.0 | 预编译 bottle | 同上 |
| libpcre2-8.so.0 | PCRE2 10.47 | 预编译 bottle | 同上 |
| libpixman-1.so.0 | pixman 0.46.4 | 预编译 bottle | 同上 |
| libz.so.1 | zlib-ng 2.3.3（compat 模式） | 预编译 bottle | 同上 |

要点：

- **libslirp 与 libqemu 是两个不同上游项目**：libslirp 原是 QEMU 源码内 `slirp/`
  子目录，2019 年 QEMU 4.2 拆分为独立库（gitlab.freedesktop.org/slirp/libslirp）。
  本构建经 meson subproject 机制（`subprojects/slirp.wrap`，固定 commit
  `26be815b…`）把独立源码拉进 QEMU 构建树，与 libqemu **同批、同工具链**编译，
  因此二进制特征高度一致，但代码与许可证各自独立。
- **5 个 bottle 不是本地编译的**：编译目标为 `arm64_ohos`（musl，链 `libc.so`
  而非 glibc），由跑在应用沙箱里的 Homebrew-ohos 从 `arm64_ohos` bottle 源下载
  安装；md5 5/5 与设备 `~/.harmonybrew/Cellar/<包>/<版本>/lib/` 原始文件逐字节
  一致，属原样拷贝。**本机没有这 5 个库的源码编译记录**，回溯源码需查
  Homebrew-ohos 的 formula / packages 元数据（安装收据 source 指向
  `.../files/Homebrew/api/internal/packages.arm64_ohos.jws.json`）。
- **时间戳佐证**：6 个依赖 so 全在 2026-09-01 23:49 同分钟落盘，晚于 libqemu 的
  22:52——"编完引擎后统一收集依赖"。
- 这批 so 面向 OHOS 设备侧（musl），与 guest 内 glibc 无关，也不能在 glibc 宿主上
  直接 dlopen。

### C. 许可证矩阵与义务提示

7 个 so 来自 7 个项目、分属 4 类许可，**不一致**：

| 文件 | 许可证 | 类别 |
|---|---|---|
| libqemu-system-aarch64.so | GPL-2.0 | 强 copyleft |
| libglib-2.0.so.0 | LGPL-2.1-or-later | 弱 copyleft |
| libintl.so.8 | LGPL-2.1-or-later | 弱 copyleft |
| libpcre2-8.so.0 | BSD-3-Clause | 宽松 |
| libpixman-1.so.0 | MIT | 宽松 |
| libslirp.so.0 | BSD-3-Clause（个别文件 MIT） | 宽松 |
| libz.so.1 | zlib License | 宽松 |

义务提示：QEMU system 模拟器**无 GPL 链接例外**，dlopen 集成分发通常仍视为衍生
作品，有源码公开义务；glib/intl 走动态链接分发不触发 LGPL 义务，**静态链入才触发**
重链接条款（这正是本文档主体静态形态在许可上更"重"的原因）；宽松类（BSD/MIT/
zlib）保留版权声明即可。zcoder 内部自用无碍；若对外分发 HAP，建议在 zcoder
`legal/` 补 third-party notices 并随附各 LICENSE 文本。

### D. 与 Rust 侧（cmd-agent / crates.io）的关系

- 这 7 个 so 全部是 C 生态产物（meson / brew / NDK clang 产出），**与 crates.io
  依赖无关**。
- cmd-agent（Rust crate，`crates/gpui_ohos/depend/ohos-qemu-agent/cmd-agent`，
  依赖 crates.io 的 nix 等及 workspace 的 virtiofsd）只负责在运行时
  `dlopen("libqemu-system-aarch64.so")`（裸 SONAME，靠 HAP libs 目录解析这批依赖）。
- virtio-fs 后端（virtiofsd crate）静态链入 cmd-agent 作为 vhost-user 线程，不产出
  任何 so/二进制，因此 images/ 里没有 virtio-fs 相关文件。
- 部署拷贝点在 zcoder `script/bundle-ohos`（约 255-257 行）。

### E. 与"全静态"目标形态的关系

两份清单并存对应两条分发路径：本文档主体（§五~六）产出全静态单文件，适合
单 .so 分发、杜绝设备端缺库脆弱点；images/ 动态形态是当前 zcoder HAP 的实际
部署（沿用 OHcode 时代依赖打包、后经本地重编的动态链接产物）。若要把实际部署
收敛到全静态：按第五节流程重编，并确保 configure 命中 PIC 静态库——harmonybrew
官方 bottle 通常不带 `.a`（需自编，如 zlib 已有 `build-zlib-pic.sh` 先例），且
静态链入会使 glib/intl 的 LGPL 义务从不触发变为需满足重链接条款（内部自用不受
影响）。
