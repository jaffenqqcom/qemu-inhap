# QEMU 鸿蒙化改造方案

**目标**：将本仓库（qemu-inhap，QEMU 11.1.50）用鸿蒙交叉工具链编译为 `libqemu-system-aarch64.so` 共享库，可被鸿蒙 HAP 通过 `dlopen` 加载运行（arm64 Linux guest，`-M virt` 机型）。

**基线**：本仓库 `master`（QEMU 11.1.50，2026-08-26 附近）。
**路线依据**：社区已验证方案 `harmoninux/qemu` → HiSH/HiVM（产物即 `libqemu-system-aarch64.so`，经 N-API 被 HAP 加载）。本方案在其基础上针对 11.1.50 与本地环境重新制定。

---

## 0. 背景与决策记录

- **Guest 架构**：arm64 Linux guest（`aarch64-softmmu`，`-M virt`）。不使用 `-M microvm`（该机型仅 x86_64，见下方澄清）。
- **交付范围**：先出本方案文档，改造点经人工逐项确认后再动代码。
- **源码基线**：本仓库 qemu-inhap 11.1.50。
- **"MicroVM" 澄清**：`-M microvm` 是 x86_64 专属机型（i386 架构，Firecracker 风格最小机），arm64 设备上不可用，且 `-m`（小写）只是内存大小参数。arm64 路线对应 `-M virt` 通用机型；社区 HiSH/HiVM 跑的就是 arm64 Linux guest。

---

## 1. 现状盘点

### 1.1 本机已就绪

- **OHOS NDK 交叉工具链**：`/mnt/linux_share/workspace/commandline-linux-arm64/sdk/default/openharmony/native/llvm/bin/`
  - `aarch64-unknown-linux-ohos-clang` / `-clang++`（内部已带 `--target=aarch64-linux-ohos --sysroot=...`）
  - sysroot 位于 `native/sysroot`，含 `libc.so`（musl）、`libz.so` + `zlib.h`、`libdl`、`libpthread`、`libm`
- **QEMU 核心依赖已交叉编译**（aarch64 OHOS/musl，位于 `/mnt/linux_share/.harmonybrew/Cellar/`）：
  - glib 2.88.3（满足 QEMU 要求 `>=2.75.3`）含 glib/gobject/gmodule/gio
  - pixman 0.46.4（满足 `>=0.21.8`）
  - pcre2 10.47（glib 的 `Requires.private`）、gettext libintl 8（glib 的 `Libs: -lintl`）
  - zlib-ng-compat 2.3.3（提供 zlib.pc）
- **Rust OHOS target** 已装（`aarch64-unknown-linux-ohos`），但 QEMU 11.1.50 的 Rust 特性默认 `disabled`，本方案不启用。
- 系统 `pkg-config` 1.9.5、`ninja`、`python3` 3.11 可用。

### 1.2 本机缺失/需处理

- **meson >= 1.5.0 未装**。harmonybrew 自带的 meson 1.12.0 shebang 指向 `/storage/Users/currentUser/.harmonybrew/opt/python@3.14/...`（设备路径，本机不存在）。需 `pip3 install meson` 或修正 shebang 后使用。
- **harmonybrew 的 `.pc` 文件 prefix 全指向设备路径** `/storage/Users/currentUser/.harmonybrew/...`，本机不存在。`glib-2.0.pc`、`pixman-1.pc`、`zlib.pc`、`libpcre2-8.pc`、`libintl.pc` 都需生成修正 prefix 的覆盖版（sed 替换为 `/mnt/linux_share/.harmonybrew`）。
- **OHOS NDK 不带任何 `.pc`**（sysroot 库无 pkgconfig 描述），zlib 需用 harmonybrew 的 zlib.pc 或自写。
- 老 `config.sub` 不识别 `aarch64-linux-ohos`（待验证 QEMU 自带版本是否已支持）。

---

## 2. 总体技术路线

```
宿主机（openEuler aarch64）
  ├─ OHOS NDK clang ── 交叉编译 ──▶ libqemu-system-aarch64.so（含 TCG JIT）
  ├─ harmonybrew glib/pixman/zlib（aarch64 musl）── 静态/动态链接进 .so
  └─ meson cross file（system=linux, cpu_family=aarch64）

鸿蒙设备（arm64-v8a HAP）
  ├─ libentry.so（NAPI 层）：dlopen("libqemu-system-aarch64.so") + dlsym("main") + 起线程
  ├─ ArkTS：startVm.ets 构造 `-M virt -cpu cortex-a57 -kernel Image -hda rootfs.qcow2 -nographic` 参数
  ├─ 运行时：QEMU serial → unix socket → 前端（hterm / VNC）
  └─ 权限：ALLOW_WRITABLE_CODE_MEMORY / LOAD_INDEPENDENT_LIBRARY / IGNORE_LIBRARY_VALIDATION / CUSTOM_SANDBOX / INTERNET
```

---

## 3. 改造点清单总览

| 阶段 | 改造对象 | 类型 | 说明 |
|---|---|---|---|
| P0 | 构建环境 | 环境 | 装 meson、修 .pc |
| P1 | `meson.build` | 源码 | 产物从 executable 改为 shared_library |
| P1 | `system/main.c` | 源码 | 保留/导出 `main` 符号 |
| P1 | `configure` / config.sub | 源码 | 识别 `aarch64-linux-ohos`（musl） |
| P2 | `tcg/`、`include/` | 源码 | OHOS 上启用 JIT（mmap W+X） |
| P2 | 若干 .c | 源码 | seccomp 白名单外 syscall 规避 |
| P3 | HAP（NAPI/ArkTS/module.json5） | 新增 | 加载与运行壳 |
| P4 | 依赖 | 依赖 | 按需交叉编译 libslirp 等 |

> 核心虚拟化功能（TCG、virt 机型、virtio、serial、块设备）**保持完整，不裁剪**。`--disable-*` 仅针对 OHOS 平台不提供的后端（GTK/SDL/VNC 等图形栈、KVM 等超管），属于平台适配，非功能裁剪。

---

## 4. P0 构建环境准备

### 4.1 安装 meson

```bash
# 系统 python3 无 meson；pip 安装（网络可用）
pip3 install --user 'meson>=1.5.0'
# 或修正 harmonybrew meson shebang：
#   sed -i '1s#.*##!/usr/bin/env python3#' /mnt/linux_share/.harmonybrew/Cellar/meson/1.12.0/bin/meson
python3 -m mesonbuild.mesonmain --version   # 验证
```

### 4.2 生成修正 prefix 的 .pc 覆盖目录

```bash
PKGOV="$HOME/qemu-ohos-pkgconfig"; mkdir -p "$PKGOV"
HB=/mnt/linux_share/.harmonybrew
for pc in glib-2.0 gmodule-export-2.0 gmodule-no-export-2.0 gobject-2.0 gio-2.0 pixman-1 zlib libpcre2-8 libintl; do
  src=$(find "$HB/Cellar" -path "*lib/pkgconfig/$pc.pc" 2>/dev/null | head -1)
  [ -n "$src" ] && sed 's#/storage/Users/currentUser/.harmonybrew#/mnt/linux_share/.harmonybrew#g' "$src" > "$PKGOV/$pc.pc"
done
export PKG_CONFIG_PATH="$PKGOV:$HB/Cellar/glib/2.88.3/lib/pkgconfig"
# 验证
pkg-config --modversion glib-2.0 pixman-1 zlib
pkg-config --cflags --libs glib-2.0   # 应指向 /mnt/linux_share/.harmonybrew 真实路径
```

---

## 5. P1 交叉编译最小 aarch64-softmmu 为 .so

### 5.1 工具链封装

OHOS NDK 已提供 `aarch64-unknown-linux-ohos-clang` 包装器（自带 `--target`/`--sysroot`）。配置时设环境变量，避免 configure 先找 `-gcc` 失败：

```bash
export LLVM=/mnt/linux_share/workspace/commandline-linux-arm64/sdk/default/openharmony/native/llvm
export CC=$LLVM/bin/aarch64-unknown-linux-ohos-clang
export CXX=$LLVM/bin/aarch64-unknown-linux-ohos-clang++
export AR=$LLVM/bin/llvm-ar
export RANLIB=$LLVM/bin/llvm-ranlib
export STRIP=$LLVM/bin/llvm-strip
export PKG_CONFIG_PATH="$HOME/qemu-ohos-pkgconfig"
```

### 5.2 configure 命令（草案，实施时按报错微调）

```bash
mkdir -p build-ohos && cd build-ohos
../configure \
  --target-list=aarch64-softmmu \
  --cross-prefix=aarch64-unknown-linux-ohos- \
  --disable-kvm --disable-whpx --disable-hvf --disable-hax --disable-accel-mpath \
  --disable-slirp --disable-gtk --disable-sdl --disable-vnc --disable-curses \
  --disable-docs --disable-tools --disable-guest-agent --disable-modules \
  --disable-werror --disable-bzip2 --disable-bochs --disable-dmg --disable-vvfat \
  --disable-vhost-user --disable-vhost-vdpa --disable-libiscsi --disable-libnfs \
  --disable-rbd --disable-curl --disable-glusterfs --disable-vde --disable-netmap \
  --disable-debug-tcg \
  --disable-stack-protector
```

- `--cross-prefix=aarch64-unknown-linux-ohos-`：configure 会拼出 `...-gcc`，不存在时按需回退 clang；若失败则依赖 `$CC` 环境变量。
- 若 config.sub 报 `unknown machine type` / `aarch64-linux-ohos` 不识别：从 GNU 上游取最新 `config.sub` 替换 `scripts/` 或仓库自带版本（harmoninux 记录过此坑）。
- 若 `host_os=linux` 触发不兼容特性（multiprocess、vfio-user、seccomp 等），用对应 `--disable-*` 关闭；`--disable-seccomp` 可选（OHOS 自身有 seccomp，QEMU 的 host 侧 seccomp 过滤用不到）。

### 5.3 meson 交叉文件

configure 会自动生成 `build-ohos/meson-private/` 下的交叉文件。关键点：

- `host_machine.system = 'linux'`，`cpu_family = 'aarch64'`，`endian = 'little'`
- **必须追加 meson 参数**（QEMU `meson.build:3` 默认 `b_staticpic=false`，非 PIC 对象无法链接成共享库）：

```bash
export MESON_ARGS="-Db_staticpic=true -Db_pie=false -Ddefault_library=shared"
```

> `-Ddefault_library=shared`：把内部静态库（libqemu-system-aarch64.fa 及各子系统）以 PIC 编译，供最终共享库链接。`-Db_pie=false`：关闭可执行文件的 PIE 链接路径，避免与 `-shared` 冲突。

### 5.4 产物改为共享库

#### 改造位置

`meson.build:4410-4454`。核心是：

```meson
emulator = executable(exe_name, exe['sources'],
           install: true,
           ...
           objects: lib.extract_all_objects(recursive: true),
           link_depends: [block_syms, qemu_syms],
           link_args: link_args,
           win_subsystem: exe['win_subsystem'])
```

其中 `lib` 是 `static_library('qemu-' + target, ...)`（`meson.build:4399`），`sources` 含 `system/main.c`（定义 `main()`）。

#### 方案 A（推荐，可控）：新增 meson 选项切换 shared_library

- 在 `meson.build` 顶部新增 `option('libqemu', type: 'feature', value: 'disabled', description: 'build QEMU as loadable shared library')`（或直接读现有 configure 传入的变量）。
- 在 `execs` 循环里：

```meson
if get_option('libqemu').enabled()
  shared_library('qemu-system-' + target_name,
             exe['sources'],
             objects: lib.extract_all_objects(recursive: true),
             dependencies: arch_deps + exe['dependencies'],
             link_depends: [block_syms, qemu_syms],
             link_args: link_args + ['-Wl,-soname,libqemu-system-' + target_name + '.so'],
             install: true,
             name_prefix: 'lib')
else
  emulator = executable(...)   # 原样保留
endif
```

- `shared_library` 默认 name_prefix `lib`，产物名即 `libqemu-system-aarch64.so`。共享库默认导出全部非 static 符号，`main` 可被 `dlsym`。
- 用 `configure --enable-libqemu`（或 `-Dlibqemu=enabled`）控制，不影响 Linux/其它平台原构建（保持 `--disable-libqemu` 默认行为一致）。

#### 方案 B（零源码改，风险高）：链接期强改

```bash
export LDFLAGS="-Wl,-shared -Wl,-soname,libqemu-system-aarch64.so"
```
让 meson 把 executable 直接链接成共享库。风险：meson 对 executable 会附加 `-pie`/`-no-pie`、`-Wl,-z,noexecstack` 等参数，与 `-shared` 冲突，且 `-shared` 属 ldflag 侵入可执行文件链接，行为脆弱。**不推荐作为主方案**，可作验证参考。

> 结论：采用方案 A，改动集中、可控、可回退，符合"不修改移植文件夹外代码、不改变其他平台编译行为"的纪律（`--enable-libqemu` 默认关闭，默认路径行为不变）。

### 5.5 musl / 平台适配

- **config.sub**：QEMU 自带 `config.sub` 版本较新，实测是否接受 `aarch64-linux-ohos`；不接受则替换最新版。
- **编译错误修复**：OHOS musl 与 glibc 差异会暴露编译问题，按 harmoninux 记录参考：
  - 多余 `const` qualifier 告警/错误（11.1.50 可能已修复，待实测）
  - `atomic16` 相关（旧分支曾修）
  - 无 `/proc` 完整视图、`sysinfo` 差异等运行时问题留到 P2
- 逐条编译错误按 `三法无果则停而查之` 处理，不屏蔽代码。

### 5.6 P1 验证

```bash
ninja -C build-ohos
# 期望产物
file build-ohos/qemu-system-aarch64            # ELF shared object（方案 A）或 PIE executable
# dlopen 冒烟：用一个小 C 程序（或 ctypes）在本机以 qemu-aarch64 用户态跑
readelf -d build-ohos/libqemu-system-aarch64.so | grep NEEDED   # libc.so、libglib 等，均为 OHOS musl
nm -D build-ohos/libqemu-system-aarch64.so | grep ' T main'
```

- 本机（openEuler aarch64）**不能直接 dlopen** OHOS musl 的 .so（依赖 musl libc.so 语义）。验证方式：
  1. 静态检查：`readelf`/`nm` 确认产物为 ET_DYN 且导出 `main`；
  2. 用户态模拟：`qemu-aarch64`（binfmt）配合 OHOS musl 环境跑 dlopen 测试程序；
  3. 真机：装入 HAP 或推到设备，用 `dlopen` 冒烟。

---

## 6. P2 TCG JIT 与沙箱适配

### 6.1 JIT 可执行内存（核心）

- QEMU TCG 需把翻译出的 guest 代码写入可执行内存（`mmap(PROT_READ|PROT_WRITE|PROT_EXEC)` 或 RW→RX mprotect）。
- OHOS 应用沙箱默认拒绝 W+X（防注入）。两层解法：
  1. **权限层**：`module.json5` 声明 `ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY`（system_basic，**仅平板/PC/2in1 可申请，手机被驳回**）。
  2. **代码层**（harmoninux "enable jit on OHOS"）：确保 TCG 的 `tcg_region_init()` / code_gen_buffer 在 OHOS 上走 mmap 可执行路径（QEMU 内部根据 `CONFIG_LINUX` 等宏选择路径，可能需要加 `target_os == ohos` 分支）。涉及 `tcg/region.c`、`include/exec/` 相关分配逻辑。
- **MAP_EXECUTABLE/MAP_JIT 标志（新确认，必改）**：HarmonyOS NEXT Beta 2 起安全策略收紧，**JIT 代码的 `mmap` 必须显式传 `MAP_EXECUTABLE`（内部等同 `MAP_JIT`），否则映射直接失败**（这是 mozjs/servo 在鸿蒙上的实际修复，华为工程师提交，对 QEMU TCG 同样适用）。因此 QEMU 分配 code_gen_buffer 的 `mmap` 需在 OHOS 下追加 `MAP_EXECUTABLE`。
- **设备分档（HiSH 已验证）**：
  - **平板/2in1**：`ALLOW_WRITABLE_CODE_MEMORY` + `MAP_EXECUTABLE`，走正常 TCG JIT。
  - **手机**：拿不到权限 → 需非 JIT 变体：`--enable-tcg-interpreter`（TCI）纯解释执行，性能损失大；或限制 guest 规模。HiSH 默认构建 `product/tablet`，Phone 版需另配模块，市场版干脆不带 JIT。
- 构建产物上：TCI 变体可只加 `--enable-tcg-interpreter` 单独出一版 `libqemu-system-aarch64-tci.so`，与 JIT 版共存，运行时按设备能力选择。

### 6.2 沙箱 syscall 白名单

- OHOS 应用 seccomp 白名单外的 syscall 会被 **SIGSYS 硬杀**（非错误码，不可 try-catch）。QEMU 运行期会用 `openat2`（glib 新版路径）、`memfd_create`、特定 `mmap` flag、`clone3` 等。
- 处置：真机跑最小 guest，用崩溃日志（`cppcrash-com.*.log`）定位 `SIGSYS(syscall number=N)`，对照 `openharmony` seccomp 白名单，白名单外的在代码层改绕行（例：`openat2` → `openat`，与 warp-ohos 处理 cap-primitives 同思路）。
- 禁 fork/exec：QEMU 部分功能（`-netdev tap`、某些 helper）依赖 fork，需 `--disable` 或走 unix socket 桥接；serial 输出重定向到 unix socket 即可（HiSH 已验证）。

### 6.3 P2 验证

- 真机加载 .so 跑 `-M virt -cpu cortex-a57 -kernel <Image> -initrd <initramfs> -nographic -serial unix:/tmp/console.sock`
- hilog 按 `qemu` 过滤查看启动日志；崩溃查 cppcrash 日志。

---

## 7. P3 HAP 集成（新增工程，非本仓库）

复用 HiSH/HiVM 骨架，或新建标准 HarmonyOS Native 工程：

- **C++ NAPI 层**（`libentry.so`，`entry/src/main/cpp/napi_init.cpp`）：
  - 导出 `startVM(argc/argv 数组)`、`checkPortUsed(port)`、`stopVM()`
  - 内部 `dlopen("libqemu-system-aarch64.so")` → `dlsym(handle, "main")` → `pthread_create` 里调用 `main(argc, argv)`；TSFN 回传状态
  - `libqemu-system-aarch64.so` 放入 `entry/libs/arm64-v8a/`，随 HAP 打包
- **依赖 .so 一并打包（新确认，必做）**：glib、pixman、zlib、pcre2、libintl 等 QEMU 的运行时依赖若以动态库形式链接，必须**全部随 HAP 打包进 `entry/libs/arm64-v8a/`**（HiSH 把 `deps/output` 的产物整体复制到 `entry/libs/<abi>/`）。加载时配合 `LOAD_INDEPENDENT_LIBRARY`（非应用自带 .so）。备选：把 glib/pixman/zlib 以静态库（harmonybrew 均有 `.a`）链进 `libqemu-system-aarch64.so`，减少打包体积与加载项——**推荐静态链接**（本机 harmonybrew 的 `.a` 齐全），文档 P1 命令已按此设计；若静态链接遇到 glib gmodule/dlopen 相关麻烦，回退到动态打包。
- **ArkTS 层**（`startVm.ets`）：构造 QEMU 命令行（`-machine virt -cpu cortex-a57 -kernel ... -hda ... -nographic -serial unix:/...`），调 `nativenapi.startVM()`；前端用 hterm（HiSH 用 ArkWeb 里的 hterm 接 serial unix socket）或 VNC
- **内核与 rootfs**：arm64 精简内核（HiSH 用 v6.12 + `arm64_virt` 配置）+ Alpine/其它 rootfs（qcow2 或 initramfs）
- **签名**：正式签名由 AGC 签发；调试用 DevEco 调试签名。

### module.json5 权限清单（P3 时逐项按需声明）

- `ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY`（TCG JIT，system_basic，平板/PC/2in1）
- `ohos.permission.kernel.LOAD_INDEPENDENT_LIBRARY`（加载 QEMU 引擎库 .so，system_basic）
- `ohos.permission.kernel.IGNORE_LIBRARY_VALIDATION`（跳过 .so 签名校验，normal）
- `ohos.permission.system.CUSTOM_SANDBOX`（QEMU 文件布局）
- `ohos.permission.INTERNET` / `GET_NETWORK_INFO`（网络，slirp/端口转发）
- 如需共享文件夹：`ohos.permission.system.READ_WRITE_USER_FILE`

每项声明三要素（name/reason/usedScene），reason 指向字符串资源。

---

## 8. 待确认/可选项

- **网络**：若需 guest 联网，交叉编译 libslirp（harmonlinux 用端口转发）；否则 `--disable-slirp`。
- **共享文件夹**：virtio-9p 需宿主侧 `CUSTOM_SANDBOX` + 9p-local（harmonlinux 曾移除 9p-local 的 fchmod），可选项。
- **Rust**：默认关闭，不引入。
- **图形输出**：arm64 guest 跑无头（serial）即可；需要图形时 HiSH 用 VNC + ArkWeb，属 P3 增强。

---

## 9. 风险与应对

1. **JIT W+X 权限**：手机端 `ALLOW_WRITABLE_CODE_MEMORY` 被驳回 → 降级 TCI 或限定平板/2in1。
2. **seccomp SIGSYS**：白名单外 syscall 硬杀 → 逐个定位改绕行（不能靠权限开放）。
3. **glib 2.88 / pixman 0.46 与 11.1.50 兼容性**：版本满足下限要求，运行时行为需真机验证。
4. **.pc prefix 漂移**：harmonybrew 的 .pc 指向设备路径 → P0 已统一用修正版覆盖目录。
5. **.so 链接期符号解析**：`-Db_staticpic=true` 后体积/性能变化，必要时仅对链接目标开 PIC（`b_staticpic` 全局覆盖为 true 最省事）。
6. **musl 差异**：逐条编译错误修复，不用屏蔽/桩函数规避。
7. **harmoninux 改动与 11.1.50 差异**：其 patch 基于旧版 QEMU，移植需逐个适配，不直接照抄。
8. **信号处理器冲突**：QEMU 安装自己的 SIGSEGV/SIGBUS 处理器（TCG guard page / SMC 检测），宿主 App 若也装信号处理器会干扰；OHOS 对宿主信号投递若有额外限制会放大此问题。P3 运行时验证，必要时协调信号链。
9. **glib 内部 dlopen/gmodule**：glib 的 gmodule 机制在静态链接 + dlopen 场景可能行为异常（模块地址解析），若静态链接 glib 遇到 gmodule 问题，回退为动态打包 glib（HiSH 即动态打包）。

---

## 10. 需要人工逐项授权确认的改动点

以下为本仓库源码改动点，逐条确认后才实施：

1. `meson.build`：新增 `libqemu` 选项 + `shared_library` 分支（默认关闭，不影响默认构建）
2. `system/main.c`：确认 `main` 符号导出方式（是否加 `qemu_entry` 包装，默认不加）
3. `configure` / `config.sub`：支持 `aarch64-linux-ohos` host 识别（如 QEMU 自带 config.sub 已支持则无需改）
4. `tcg/region.c` 等：OHOS 下 mmap 可执行内存路径分支（P2，需先有真机）
5. 运行期 syscall 规避补丁（P2，按崩溃定位后逐条提）
6. （P3）HAP 工程为新增目录，不落入本仓库

> 原则重申：不改本仓库任何无关代码；不屏蔽/删减功能；不改其他平台编译行为；新增代码全英文注释；异常路径必带 error/warn 日志。

---

## 11. 实施进度（2026-08-27）

### 已完成

- **P0 环境**：meson 1.12.0（harmonybrew 包 + PYTHONPATH 包装）、修正 prefix 的 `.pc` 覆盖目录、`~/qemu-ohos/env.sh`。构建根目录必须在本地磁盘（virtiofs 共享盘对 chmod/chown/utimensat 返回 EPERM，见第 12 节）。
- **P1 源码改造**（已应用，默认关闭不影响 Linux 构建）：
  - `meson_options.txt`：新增 `libqemu` 选项
  - `meson.build`：静态库 `pic: get_option('libqemu').enabled()` + 共享库目标
  - `util/cpuinfo-aarch64.c`：`#ifdef HWCAP2_CSSC` 保护（OHOS sysroot 无该宏）
  - `configs/devices/aarch64-softmmu/ohos.mak`：最小设备集
- **P1 交叉编译通过**：`libqemu-system-aarch64.so`（67MB）+ `qemu-system-aarch64`（65MB），依赖精简为 libslirp/libz/libpixman/libglib/libc（musl），`main` 已导出。

### 编译过程关键修复（已沉淀进脚本）

1. `-Wno-unused-command-line-argument`：OHOS clang 自动加 `-L<clang>/lib/<triple>`，纯编译阶段触发 meson 的 `-Werror=unused-command-line-argument` → 用 clang 包装脚本在参数末尾追加。
2. `-D__user=`：OHOS sysroot 的 `linux/keyctl.h` 用 `__user` 注解但未定义（仅在 linux/compiler.h）→ 全局定义为空。
3. `--disable-vhost-*`：libvhost-user 的 standard-headers（`_LINUX_` guard）与 OHOS sysroot（`_UAPI_` guard）的 virtio_ring.h 冲突 → 禁用 vhost 系列避免编译。
4. `--disable-libusb`：libusb 被宿主 pkg-config 拉到（glibc 版），禁用避免混入。
5. allnoconfig 下 `ARM_VIRT` 必须显式 `CONFIG_ARM_VIRT=y`（否则 virt 机型不编译）。
6. `-Db_staticpic=true`：QEMU 默认 `b_staticpic=false`，非 PIC 静态库无法链入共享库。

### 交付物

`scripts/ohos/output/`：`libqemu-system-aarch64.so` + 运行时依赖（libslirp/libglib/libgmodule/libpixman/libz/libpcre2/libintl），全部 aarch64 OHOS/musl。HAP 打包时放入 `libs/arm64-v8a/` 并 `dlopen`。

### 构建脚本（在仓库内）

`scripts/ohos/`：`env.sh`（环境 + 生成包装脚本/.pc）、`sync-src.sh`（同步源码到本地副本）、`configure-ohos.sh`（配置）、`build-ohos.sh`（构建）、`dlopen-test.c`（真机冒烟测试）。

## 12. 为什么构建产物必须在本地磁盘

`/mnt/linux_share` 是 virtiofs 共享盘，对非 root 的 `chmod/chown/utimensat` 返回 EPERM。QEMU 构建的 configure 会在构建目录建 venv（pip 安装需 chmod）、把 `python/` 包以 editable 方式 pip 安装进源码树（setuptools 需 os.utime），ninja 写产物也依赖这些元数据操作。这些在共享盘上全部失败（与 warp-ohos 构建到 `$HOME/.ohos-mounts` 同理）。因此：

- 构建根目录 `QEMU_OHOS_BUILD` 默认 `/tmp/qemu-ohos`（可用环境变量覆盖），源码副本与构建产物都在本地。
- 仓库内只放脚本与最终交付物（`.so` 及依赖），不放构建中间产物。
- 共享盘 `python/qemu.egg-info` 为早期失败构建的残留，需人工清理（本仓库规则禁止脚本删除目录）。
